{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome - GPTars Website",
            "url": "http://localhost:8000/index.html",   // ← EXACTE BESTAND
            "webRoot": "${workspaceFolder}",             // ← JUIST: index.html zit in root
            "sourceMaps": true
        }
    ]
}