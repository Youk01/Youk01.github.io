{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome - GPTars Website",
            "url": "http://localhost:8080/index.html",  // ← Update naar 8080
            "webRoot": "${workspaceFolder}"
        }
    ]
}